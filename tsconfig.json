{
  "compilerOptions": {
    // ES2020 au minimum : les littéraux BigInt sont indispensables pour parler
    // à la chaîne, où tout montant est un uint256.
    "target": "ES2020",
    "lib": [
      "dom",
      "dom.iterable",
      "esnext"
    ],
    "allowJs": true,
    "skipLibCheck": true,
    "strict": true,
    "noEmit": true,
    // Les tests et scripts en .mts sont exécutés par Node, qui exige
    // l'extension dans l'import — alors que tsc la refuse par défaut. Sans code
    // émis, la permettre ne change rien à ce que le bundler produit : les
    // fichiers du site importent toujours sans extension, comme avant.
    "allowImportingTsExtensions": true,
    "esModuleInterop": true,
    "module": "esnext",
    "moduleResolution": "bundler",
    "resolveJsonModule": true,
    "isolatedModules": true,
    "jsx": "preserve",
    "incremental": true,
    "plugins": [
      {
        "name": "next"
      }
    ],
    "paths": {
      "@/*": [
        "./*"
      ]
    }
  },
  "include": [
    "next-env.d.ts",
    "**/*.ts",
    "**/*.tsx",
    ".next/types/**/*.ts",
    ".next/dev/types/**/*.ts",
    "**/*.mts"
  ],
  "exclude": [
    "node_modules",
    "contracts"
  ]
}
